/* eslint-disable react/prop-types */
const Tag = ({name}) => {
    return (
        <span className="px-2 text-sm bg-accent/10 font-rosario text-accent rounded-full">{name}</span>
    );
};

export default Tag;